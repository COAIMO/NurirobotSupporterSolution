@page "/multiple"
@inject IJSRuntime JS

<div id="viewer"></div>

@code
{ protected override async Task OnInitializedAsync()
    {
        await JS.InvokeVoidAsync("multiplepage", null);
        await Task.CompletedTask;
    } }
