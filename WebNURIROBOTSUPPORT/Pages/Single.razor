@page "/single"
@inject IJSRuntime JS

<div id="viewer"></div>

@code
{ protected override async Task OnInitializedAsync()
    {
        await JS.InvokeVoidAsync("singlepage", null);
        await Task.CompletedTask;
    } }
