@page "/macro"
@inject IJSRuntime JS

<div id="viewer"></div>

@code
{ protected override async Task OnInitializedAsync()
    {
        await JS.InvokeVoidAsync("macropage", null);
        await Task.CompletedTask;
    } }
